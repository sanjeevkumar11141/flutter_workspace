import 'package:flutter/material.dart';
class InspectionFlowsPage extends StatelessWidget {
  const InspectionFlowsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Inspection Flows Page',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}